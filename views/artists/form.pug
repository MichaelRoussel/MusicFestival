div
  form(action=(artist ? `/artists/update` : "/artists"), method="POST")
    if artist
      input(type="hidden", name="id", value=artist.id)

    .form-group
      label Name
      input.form-control(name="artist[name]", required, value=(artist ? artist.name : null))

    .form-group
      label Genre
      input.form-control(name="artist[genre]", required, value=(artist ? artist.genre : null))
    
    .form-group
      button.btn.btn-dark(type="submit") Submit